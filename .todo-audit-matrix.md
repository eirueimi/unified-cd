# Active Backlog Audit Matrix

Audit baseline: `1e46459` (2026-07-26)

This is a temporary review artifact. Every row must be decided before the
active backlog is rewritten, and this file must be removed from the final
tree.

## Decision Rules

- Resolved: current implementation and tests cover the legacy problem.
- Accepted behavior: current behavior is intentional and documented.
- Environment-only: the observation belongs to a historical validation setup.
- Open: the problematic behavior is still present.
- Partial: a concrete part remains after verified fixes.
- Needs verification: static evidence is insufficient; the retained task must
  state an exact experiment and success condition.

## Candidate Ledger

| Key | Legacy scope | Wave | Outcome | Evidence | Final action |
|---|---|---:|---|---|---|
| 1 | Step failure or skip can wedge a run | Wave 1 | Resolved | `internal/store/migrations/001_init.up.sql` constraint `step_reports_status_check` accepts `Skipped` and `WaitingApproval`; `internal/agent/retry.go` `retryUntilSuccess` stops on permanent 4xx errors; `test/e2e/failing_step_test.go` `TestFailingStepSkipsRestAndFailsRun` proves a failed first step leaves the next `Skipped` and the run terminal `Failed`. | Remove from active backlog |
| 2 | Required/default parameter enforcement | Wave 1 | Resolved | `internal/controller/params.go` `resolveParams` rejects missing required inputs and injects defaults; `internal/controller/api_runs.go` run creation, `internal/controller/api_webhooks.go` webhook ingress, and `internal/controller/scheduler.go` schedule firing all call it. `internal/controller/params_test.go`, `api_runs_test.go`, `api_webhooks_test.go`, and `scheduler_schedule_test.go` cover missing required inputs and defaults; `docs/jobs.md` input fields match the runtime contract. | Remove from active backlog |
| 3 | Conditional syntax mismatch and fail-open behavior | Wave 1 | Partial | `docs/jobs.md` table under `Available variables` still lists Go-template expressions `{{ .Params.NAME }}` and `{{ .Steps.NAME.Outputs.KEY }}` as available in `if`, contradicting its CEL section. `internal/agent/orchestrator.go` `RunClaim` passes `step.If` directly to `internal/dsl/condition.go` `EvalCondition`, which returns true with an error on CEL compile/evaluation failure; `internal/dsl/condition_test.go` `TestEvalCondition_InvalidExpr` and `TestEvalCondition_NonBoolResult` require that fail-open result. | Retain as 3 |
| 4 | Standard-agent artifact upload relative paths | Wave 1 | Resolved | `internal/agent/orchestrator.go` `executeUploadArtifact` resolves paths through `ExecBackend.ResolveArtifactPath`; `internal/agent/backend_host.go` `hostBackend.ResolveArtifactPath` resolves against the run workspace via `resolveWorkspacePath`. `internal/agent/agent_test.go` `TestAgent_UploadArtifact_RelativePath` uploads `out.txt` from the workspace while the process cwd lacks it; `docs/jobs.md` documents relative artifact paths. | Remove from active backlog |
| 5 | Approval visibility in run show | Wave 1 | Resolved | `internal/agent/approval.go` `WaitForApproval` reports `WaitingApproval`, allowed by `step_reports_status_check`; `internal/controller/planned_steps.go` `plannedSteps` and `mergedRunSteps` expose unreported approval steps as `Pending`, and `internal/cli/run.go` `newRunShowCmd` prints the steps endpoint response. `internal/agent/approval_test.go` covers approval reporting/flow, while `internal/controller/planned_steps_test.go` covers planned-step merging and the steps API. | Remove from active backlog |
| 6 | parallel-steps example apply validity | Wave 2 | Resolved | `examples/jobs/parallel-steps.yaml` now represents each concurrent stage as a `parallel:` entry with named child steps; `internal/dsl/examples_parse_test.go` `TestExamplesParse` walks every job example and parses each `kind: Job` through `dsl.Parse`, including this file. | Remove from active backlog |
| 7 | CLI flag spelling in examples and docs | Wave 2 | Resolved | `internal/cli/run.go` `newRunTriggerCmd` registers the repeatable `--param` flag; `examples/jobs/params.yaml`, `docs/cli.md` `run trigger`, `docs/jobs.md` parameter examples, and `docs/getting-started.md` all use repeated `--param key=value`, with no current `--params` usage outside historical planning documents. | Remove from active backlog |
| 8 | Artifact step display name | Wave 2 | Needs verification | `internal/agent/orchestrator.go` `executeUploadArtifact` and `executeDownloadArtifact` include `StepName: step.DisplayName()` and `StageIndex: step.StageIndex` in Running and terminal reports; `internal/agent/agent_test.go` `TestAgent_UploadArtifact_RelativePath` asserts every upload report is named `uploadOut` instead of falling back to `step[0]`. The download half has only static implementation evidence. Add an exact `executeDownloadArtifact` test that captures its Running and terminal reports; success requires both reports to have `StepName` equal to the configured download display name and `StageIndex` equal to that step's configured stage index. | Retain as 8 |
| 9 | Workspace reuse between runs | Wave 2 | Accepted behavior | `internal/agent/workspace.go` `claimWorkDir` partitions slot workspaces by sanitized job name, while `prepareWorkspace` intentionally preserves same-job files unless cleaning is requested or execution mode changes; `internal/agent/workspace_test.go` covers job-scoped paths, carry-over, cleaning, and mode-flip reset. `docs/agents.md` `Workspace lifecycle` explicitly documents same-job carry-over, `--clean-workspace`, `podTemplate.cleanWorkspace`, and the secret-file risk. | Remove from active backlog |
| 9c | Windows cancellation and orphaned child processes | Wave 2 | Partial | `internal/agent/exec_windows.go` `assignJob` uses a Job Object with `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE`, and `internal/agent/runner_test.go` `TestRunStep_CancelKillsGrandchild` verifies the exercised process tree stops; `internal/agent/orchestrator.go` reports an interrupted step as `Cancelled`, covered by `internal/agent/agent_cancel_test.go` `TestExecuteRun_CancelledStepReportsCancelledStatus` and documented in `docs/jobs.md`. However, `internal/agent/exec_tree.go` `runTreeKilled` assigns the Job Object only after `cmd.Start` and explicitly retains both a child-spawn escape window and a single-process fallback when assignment fails, so orphan prevention is not complete. | Retain as 9c |
| 10 | Cancelled-run log visibility in the Web UI | Wave 2 | Needs verification | `web/src/routes/RunDetail.svelte` has a single reactive `runID` initialization path and always starts one SSE connection, including for terminal `Cancelled` runs; `internal/controller/sse.go` `handleRunEvents` replays existing logs before emitting any terminal status. `web/src/routes/RunDetail.test.js` pins one `/events` connection per run but exercises a Running run with an empty stream, while `internal/controller/api_runs_test.go` `TestAPI_RunEvents_SSE_ReceivesExistingLogs` backfills a Succeeded run without Web rendering. Add an exact component or browser experiment that loads a pre-cancelled run whose SSE backfill contains historical log lines and then closes with terminal status; success requires those historical lines to remain rendered in the run detail after terminal closure. | Retain as 10 |
| 11 | CLI run cancel command | Wave 2 | Resolved | `internal/cli/run.go` `newRunCmdWithClient` registers `newRunCancelCmd`, which POSTs `/api/v1/runs/{id}/cancel`; `internal/cli/run_test.go` `TestRunCancel_Success` verifies method, path, authorization, and confirmation output. `docs/cli.md` `run cancel`, `docs/troubleshooting.md`, and `docs/operations.md` document the command and cancellation behavior. | Remove from active backlog |
| 12 | Agent inventory recovery after controller database loss | Wave 2 | Partial | `internal/controller/api_agent.go` `handleAgentClaim` calls `store.UpsertAgentOnClaim` before polling, and `internal/controller/api_agent_test.go` `TestAgentAPI_Claim_UpsertsUnregisteredAgent` proves an authenticated agent missing only its inventory row reappears in `ListAgents`; `internal/store/postgres.go` `UpsertAgentOnClaim` preserves richer registration data. A fresh database also loses the credential hashes listed in `docs/operations.md`; `internal/controller/agent_auth.go` authenticates before the claim handler via `GetAgentCredentialForAuth`, so claim-time recovery cannot run until the agent is re-enrolled. `docs/operations.md` both promises automatic reappearance on a fresh database and separately requires re-enrollment, leaving the full-loss recovery contract unresolved. | Retain as 12 |
| A | Orphaned-run reaper | Wave 2 | Resolved | `cmd/controller/main.go` starts `controller.RunStuckRunReaper`; `internal/controller/stuckrun_reaper.go` leader-elects, finds stale or missing-agent Running runs through `store.ListStuckRunIDs`, marks each `Failed`, releases locks through `MarkRunFinished`, and cascade-cancels descendants. `internal/controller/stuckrun_reaper_test.go` covers leader/follower behavior, `internal/store/postgres_stuckrun_test.go` covers selection, and `docs/high-availability.md` `Orphaned-Run Recovery` documents timings and fail-not-requeue semantics. | Remove from active backlog |
| 13 | Default Kubernetes sidecar image pullability | Wave 3 | Needs verification | `internal/k8sagent/config.go` `defaultSidecarImage` is digest-pinned and supplied by `DefaultConfig`; `manifests/install.yaml` omits `sidecarImage` and consumes it. `TestDefaultSidecarImageIsDigestPinned` proves syntax, not publication, and `docs/configuration.md` plus `docs/kubernetes-integration.md` incorrectly advertise a mutable `:latest` default. Derive the exact image with `$m=[regex]::Match((Get-Content -Raw internal/k8sagent/config.go),'const\s+defaultSidecarImage\s*=\s*"([^"]+)"'); if (-not $m.Success){throw 'defaultSidecarImage not found'}; $image=$m.Groups[1].Value; $digest=$image.Split('@')[-1]`. Run the bounded credential-isolated pull as `$dockerCfg=Join-Path ([IO.Path]::GetTempPath()) ('ucd-anon-'+[guid]::NewGuid().ToString('N')); try {[void](New-Item -ItemType Directory -Path $dockerCfg); Set-Content -LiteralPath (Join-Path $dockerCfg 'config.json') -Value '{"auths":{}}' -Encoding ascii; $psi=[Diagnostics.ProcessStartInfo]::new(); $psi.FileName='docker'; $psi.UseShellExecute=$false; foreach($arg in @('--config',$dockerCfg,'pull',$image)){$psi.ArgumentList.Add($arg)}; $p=[Diagnostics.Process]::Start($psi); if (-not $p.WaitForExit(120000)){$p.Kill($true);throw 'docker pull timed out'}; if ($p.ExitCode -ne 0){throw "docker pull exited $($p.ExitCode)"} } finally {$resolvedCfg=[IO.Path]::GetFullPath($dockerCfg); $resolvedTmp=[IO.Path]::GetFullPath([IO.Path]::GetTempPath()); if (-not $resolvedCfg.StartsWith($resolvedTmp,[StringComparison]::OrdinalIgnoreCase) -or [IO.Path]::GetFileName($resolvedCfg) -notlike 'ucd-anon-*'){throw 'unsafe Docker cleanup target'}; if (Test-Path -LiteralPath $resolvedCfg){Remove-Item -LiteralPath $resolvedCfg -Recurse -Force}}`. Separately run `$ns='ucd-sidecar-pull-'+[guid]::NewGuid().ToString('N').Substring(0,8); try {kubectl create namespace $ns; if ($LASTEXITCODE -ne 0){throw 'namespace creation failed'}; kubectl -n $ns create serviceaccount pull-check; if ($LASTEXITCODE -ne 0){throw 'ServiceAccount creation failed'}; $pullSecrets=kubectl -n $ns get serviceaccount pull-check -o jsonpath='{.imagePullSecrets}'; if ($LASTEXITCODE -ne 0 -or -not [string]::IsNullOrWhiteSpace($pullSecrets)){throw 'imagePullSecrets is not empty'}; kubectl -n $ns run pull-check --restart=Never --image=$image --image-pull-policy=Always --overrides='{"spec":{"serviceAccountName":"pull-check"}}' --command -- unified-sidecar idle; if ($LASTEXITCODE -ne 0){throw 'Pod creation failed'}; kubectl -n $ns wait --for=condition=Ready pod/pull-check --timeout=120s; $waitExit=$LASTEXITCODE; kubectl -n $ns get pod pull-check -o 'custom-columns=PHASE:.status.phase,WAITING:.status.containerStatuses[0].state.waiting.reason,IMAGE_ID:.status.containerStatuses[0].imageID'; if ($LASTEXITCODE -ne 0){throw 'pull-state inspection failed'}; $imageID=kubectl -n $ns get pod pull-check -o jsonpath='{.status.containerStatuses[0].imageID}'; if ($LASTEXITCODE -ne 0){throw 'image ID inspection failed'}; kubectl -n $ns describe pod pull-check; if ($LASTEXITCODE -ne 0){throw 'Pod event inspection failed'}; if ($waitExit -ne 0){throw 'Pod did not become Ready'}; if ($imageID -notlike "*@$digest"){throw 'running image digest mismatch'} } finally {if ($ns -notmatch '^ucd-sidecar-pull-[0-9a-f]{8}$'){throw 'unsafe namespace cleanup target'}; kubectl delete namespace $ns --ignore-not-found --wait=false}`. Success requires empty ServiceAccount pull secrets, Docker exit 0, Pod `Ready`, no waiting pull reason, and an image ID ending in `@$digest`; timeout/non-zero exit or `ErrImagePull`/`ImagePullBackOff` is failure. On success, update both docs to the verified digest-derived default; on failure, publish or correct a deployable digest default, repeat both checks, and only then reconcile both docs. | Retain as 13 |
| 14 | Distroless sidecar versus shell transfer | Wave 3 | Resolved | `internal/k8sagent/backend.go` `UploadArtifact` and `DownloadArtifact` construct explicit `unified-sidecar artifact ...` argv and call `sidecarExecArgv`; `internal/k8sagent/executor.go` `ExecStepArgv` sends that argv directly through Kubernetes exec without a shell. `internal/k8sagent/orchestrate_test.go` `TestOrchestrate_UploadArtifactDispatchesToSidecar` and `TestOrchestrate_DownloadArtifactDispatchesToSidecar` assert the exact argv, and `docs/kubernetes-integration.md` documents the static distroless binary protocol. | Remove from active backlog |
| 15 | Kubernetes working directory | Wave 3 | Resolved | `internal/k8sagent/podbuilder.go` `injectWorkspace` sets the primary `job` container `WorkingDir` to the workspace mount and rejects a divergent configured value; `internal/k8sagent/scopepod.go` sets scope containers to `scopeMountPath`. `internal/k8sagent/podbuilder_test.go` `TestBuildPod_ContainersWorkingDirIsWorkspace` covers `/workspace`, a custom mount, matching and divergent configured directories, while `docs/kubernetes-integration.md` documents workspace-relative execution. | Remove from active backlog |
| 16 | Kubernetes single-file artifact upload and failure reporting | Wave 3 | Resolved | `internal/artifact/store.go` `WriteTarZstd` handles a single file relative to its parent and `StreamTarZstd` propagates producer errors; `internal/artifact/store_test.go` `TestUploadDownload_SingleFile`, `TestStreamTarZstd_SingleFile`, and `TestStreamTarZstd_ProducerErrorPropagates` cover both behavior and failure. `cmd/unified-sidecar/run.go` `runArtifact` returns non-zero on upload errors, `internal/k8sagent/backend.go` `UploadArtifact` rejects non-zero sidecar exits, and `internal/k8sagent/orchestrate_test.go` `TestOrchestrate_ArtifactExecFailureFailsRun` requires the step and run to become `Failed`. | Remove from active backlog |
| 21 | Sidecar idle mode without S3 | Wave 3 | Resolved | `cmd/unified-sidecar/main.go` supplies a lazy object-store provider, and `cmd/unified-sidecar/run.go` `run` dispatches `idle` before invoking it, so missing `UNIFIED_S3_*` values do not terminate the resident sidecar. `cmd/unified-sidecar/run_test.go` `TestRun_Idle_NeverInvokesProvider_DegradedMode`, `TestRun_CacheSubcommand_ProviderError_FailsLoudly`, and `TestRun_ArtifactSubcommand_ProviderError_FailsLoudly` cover the degraded idle path and explicit transfer failures; `internal/k8sagent/podbuilder.go` starts the sidecar as `unified-sidecar idle`. | Remove from active backlog |
| 17 | Schedule apply support | Wave 3 | Resolved | `internal/cli/apply.go` `applyOneDocument` dispatches `kind: Schedule` to `/api/v1/schedules/` with `api.ApplyScheduleRequest`, and its dry-run branch uses `dsl.ParseSchedule`; `internal/cli/apply_test.go` `TestApplyScheduleDocument` verifies the endpoint and request YAML. `internal/cli/schedule.go` provides list/delete commands, while `examples/self-monitoring/schedule.yaml`, `docs/cli.md`, and `docs/resources.md` use and document `apply -f` for Schedule resources. | Remove from active backlog |
| 18 | Artifact download default destination | Wave 3 | Resolved | `internal/cli/artifact.go` `newArtifactDownloadCmd` defaults `--dest` to `.`, and `internal/artifact/targz.go` `ExtractTarZstd` resolves the destination to an absolute path before enforcing its traversal boundary. `internal/artifact/targz_test.go` `TestExtractTarZstd_RelativeDestDot` proves extraction into `.`, while `TestExtractTarZstd_RelativeDest_StillRejectsTraversal` proves the fix preserves traversal rejection; `docs/cli.md` documents the current-directory default. | Remove from active backlog |
| 19 | call step self-deadlock | Wave 3 | Accepted behavior | `internal/agent/callstep.go` `ExecuteCallStep` intentionally waits for the child while the parent retains its claim, bounded by `timeoutMinutes` when configured. `docs/jobs.md` `Slot deadlock` explicitly requires spare normal capacity, a different child pool, a timeout, or `spec.detached: true`; `internal/agent/agent.go` runs detached claims in a separate default-16 `MaxDetachedConcurrent` pool, with separation covered by `internal/agent/agent_detached_test.go` `TestAgent_ClaimsDetachedOutsideMaxConcurrent` and detached parsing covered by `internal/dsl/parse_test.go`. The unchanged single-slot behavior is now an explicit capacity contract with an opt-in orchestrator mode rather than an undocumented product gap. | Remove from active backlog |
| 20b | CLI configuration precedence | Wave 3 | Resolved | `internal/cli/root.go` `resolveConfig` applies config file, then `UNIFIED_SERVER`/`UNIFIED_TOKEN`, then `--server`/`--token`, yielding flag > environment > config. `internal/cli/root_test.go` `TestResolveConfig_EnvOverridesConfigFile`, `TestResolveConfig_FlagOverridesEnv`, and `TestResolveConfig_FallsBackToConfigFileWhenNoOverrides` cover all levels, and `docs/cli.md` documents the same order. | Remove from active backlog |
| 20 | SSO direct hash-route rendering | Wave 3 | Resolved | `web/src/lib/api.js` `initAuth` sets `authReady` only after the session checks finish, and `web/src/App.svelte` renders a visible loading state until `authReady` before mounting `Router`. `web/src/App.test.js` `App — deep link render ordering (TODO #20)` hard-navigates to `#/agents` with a pending SSO session check, asserts the body is not blank, then asserts the routed view appears after authentication resolves. | Remove from active backlog |
| 22 | Windows Job Object handle lifecycle | Wave 4 | Resolved | `internal/agent/exec_tree.go` always defers `cleanupTree`; Windows `takeJobHandle` removes ownership before closing, making cleanup single-owner. `internal/agent/exec_tree_test.go` covers normal completion, repeated completion, and cancellation without leaking or double-closing the Job Object. | Remove from active backlog |
| 23 | Agent label deletion on re-registration | Wave 4 | Resolved | `internal/store/postgres.go` `UpsertAgent` replaces registration labels, while `UpsertAgentOnClaim` intentionally merges claim-time inventory. `TestAgentAPI_Register_RemovesDroppedLabel` and `TestUpsertAgent_ReRegistrationReplacesLabels` prove that re-registration removes a dropped label. | Remove from active backlog |
| 25 | AppSource first-sync prune after upgrade | Wave 4 | Resolved | Migration `003_appsource_managed_resources` backfills qualified managed-resource names, and `internal/controller/appsource_reconciler.go` protects legacy bare names on the first upgraded sync. `TestReconciler_LegacyBareJobNameNotPrunedOnUpgrade` plus `postgres_renamejob_test.go` cover upgrade pruning, qualified rename, idempotency, and history preservation. | Remove from active backlog |
| 26 | Artifact-name path traversal | Wave 4 | Resolved | `internal/artifact/store.go` `ArtifactKey` rejects unsafe path segments, and `internal/dsl/parse.go` rejects unsafe artifact names before execution. Their tests cover traversal attempts and valid nested storage behavior. | Remove from active backlog |
| 27 | Heartbeat lifecycle while draining | Wave 4 | Resolved | `internal/agent/agent.go` binds heartbeat lifetime to `runCtx`, not the claim loop context, so draining does not stop heartbeats for an active run. `TestAgent_HeartbeatContinuesDuringDrain` and heartbeat stop/join tests prove continuation during drain and termination after run completion. | Remove from active backlog |
| 28 | Pod GC handling of controller errors | Wave 4 | Resolved | Store lookup maps only `pgx.ErrNoRows` to `ErrRunNotFound`; the controller returns 404 only for that sentinel, and `internal/k8sagent/podgc.go` deletes a Pod only for typed HTTP 404. `podgc_test.go` distinguishes confirmed absence from connection/transient failure. | Remove from active backlog |
| 29 | Database migration coverage for pre-squash installations | Wave 4 | Accepted behavior | Startup calls `verifySchema` after migrations, and `internal/store/verify.go` fails fast on migration-number or schema-sentinel drift; tests cover every sentinel and missing columns/indexes. `docs/operations.md` and `docs/troubleshooting.md` explicitly state that automatic pre-squash migration is unsupported and prescribe the manual bridge/recovery path. | Remove from active backlog |
| 30 | Template shell argument quoting | Wave 4 | Accepted behavior | Current templates build argv with quoted `set -- "$@"` additions and escape or encode interpolated values. The remaining `extra_args` in rsync and Unity templates are intentionally documented as space-separated shell words and are expanded with globbing disabled, so metacharacters become argv data rather than being re-evaluated as shell syntax. | Remove from active backlog |
| 31 | Matrix cardinality limit before Cartesian allocation | Wave 4 | Resolved | `internal/dsl/matrix.go` checks multiplication overflow and the configured cardinality cap before allocating the Cartesian product. `TestEvalMatrix_CapExceeded_BailsBeforeFullMaterialization` and within-cap cases pin both the early bailout and normal expansion. | Remove from active backlog |
| 32 | Cache-path expansion parity | Wave 4 | Resolved | Shared orchestration in `internal/agent/orchestrator.go` handles both agent backends: template parse/expansion errors fail the run, while a valid empty path or key is warned and skipped. Host and Kubernetes cache tests cover the same cases. | Remove from active backlog |
| 32a | Empty parameters bypassing defaults | Wave 4 | Resolved | `internal/controller/params.go` treats an explicit empty value as unset when a default exists and rejects it for a required parameter without a default. `params_test.go` covers fallback, preservation for optional no-default inputs, and the required error. | Remove from active backlog |
| 32b | Standard-agent cache paths relative to process cwd | Wave 4 | Resolved | `internal/agent/backend_host.go` resolves cache paths through workspace-confined path resolution in both native and isolated modes. Host backend tests prove relative cache paths join the run workspace rather than the controller process cwd. | Remove from active backlog |
| 32c | Kubernetes cache path traversal | Wave 4 | Resolved | `internal/k8sagent/backend.go` resolves cache paths through the shared slash-path containment guard. Kubernetes backend and path-guard tests reject absolute and parent-traversal paths while accepting workspace-relative cache paths. | Remove from active backlog |
| 33a | SSO round-trip deep-link preservation | Wave 4 | Resolved | `web/src/lib/api.js` saves and restores only safe `#/` hashes, and `web/src/App.svelte` records the route before SSO then restores it after authentication. `App.test.js` covers exact deep-link restoration, clearing after use, and the no-saved-route case. | Remove from active backlog |
| 33b | AppSource sync status recovery | Wave 4 | Resolved | The leader-elected AppSource sync reaper is started by `cmd/controller/main.go` and resets stale `Syncing` rows to `Failed` while clearing the unusable commit marker. Store tests cover stale, fresh, and already-synced records. | Remove from active backlog |
| 33c | Credential-bearing AppSource repository URL disclosure | Wave 4 | Partial | Reconciler and managed-resource errors pass through `redactURLCredentials`, with tests proving credentials do not enter `lastError`. However, `ValidateGitRepoURL` still accepts URL userinfo, the AppSource API returns the raw `spec.repoURL`, and the Web list renders it, so credentials can still be disclosed through read surfaces. | Retain as 33c |
| 33d | Late finish and step reports after terminal state | Wave 4 | Partial | Finish-run writes use a compare-and-set and report `alreadyFinalized`; the known already-terminal step-report path also avoids a write. The active-run step-report path still reads status before a separate unconditional `UpsertStepReport`, leaving a race where finalization between those operations permits a stale write. | Retain as 33d |
| 33e | Published quickstart image health-check compatibility | Wave 4 | Needs verification | The production Compose file uses `ghcr.io/eirueimi/unified-cd-controller:${UNIFIED_CD_VERSION:-latest}` with a `wget` healthcheck, and the current runtime Dockerfile installs BusyBox `wget`, but source inspection cannot prove the published `latest` image matches it. Run `$image='ghcr.io/eirueimi/unified-cd-controller:latest'; $project='ucd-health-audit-'+[guid]::NewGuid().ToString('N').Substring(0,8); $env:UNIFIED_CD_VERSION='latest'; try { docker pull $image; if ($LASTEXITCODE -ne 0){throw 'image pull failed'}; docker compose -p $project -f deployments/docker/docker-compose.yaml up -d --no-build --wait --wait-timeout 120 postgres garage controller; if ($LASTEXITCODE -ne 0){throw 'compose health check failed'}; $cid=docker compose -p $project -f deployments/docker/docker-compose.yaml ps -q controller; $health=docker inspect --format '{{.State.Health.Status}}' $cid; if ($health -ne 'healthy'){throw "controller health is $health"} } finally { docker compose -p $project -f deployments/docker/docker-compose.yaml down --volumes --remove-orphans; Remove-Item Env:UNIFIED_CD_VERSION -ErrorAction SilentlyContinue }`. Success requires pull and Compose wait exit zero and inspected health `healthy`; otherwise publish or correct the image and repeat. | Retain as 33e |
| 34 | Stale failing automated tests | Wave 4 | Resolved | Phase 8 E2E now configures the deny-by-default OIDC role explicitly, and Web auth tests assert setup visibility in the component that owns it while email/logout behavior is covered in `App.test.js`. The focused Phase 8 E2E and complete Web test suite pass. | Remove from active backlog |
| 35a | Heartbeat cancellation assertion | Wave 4 | Resolved | `internal/agent/heartbeat_test.go` joins the heartbeat goroutine, settles the in-flight request, and then requires the request count to remain exactly unchanged after cancellation. The drain test also proves heartbeat termination after the active run returns. | Remove from active backlog |
| 35b | AppSource migration-path coverage | Wave 4 | Resolved | Migration backfill and reconciler tests exercise a legacy bare managed Job across the first upgraded sync, while `postgres_renamejob_test.go` covers simple and qualified renames, idempotency, and retained run history. | Remove from active backlog |
| 35c | Matrix variant output aggregation coverage | Wave 4 | Resolved | `test/e2e/matrix_smoke_test.go` runs a real controller, agent, and PostgreSQL matrix with three variants and asserts promoted run output JSON contains each variant key/value while excluding the omitted combination. Store tests separately prove variant-keyed rows do not overwrite one another. | Remove from active backlog |
| 35d | Human artifact-download authorization coverage | Wave 4 | Open | Artifact download tests prove agent-owner success, missing-artifact behavior, and unauthenticated rejection, but no successful PAT/session/OIDC human download passes through the real router. Add a seeded artifact test using an allowed human role and assert 200 with exact bytes, while unauthenticated and wrong-agent requests remain rejected. | Retain as 35d |
| 35e | CLI HTTP method assertions | Wave 4 | Partial | `captureTransport` records methods and selected mutation-command tests assert them, including AppSource sync/delete, Job/Webhook delete, run cancel, and enrollment. Most CLI command tests still assert path/body/output without an exact method, leaving GET/POST method regressions undetected; add a table-driven method-and-path contract for every request constructor. | Retain as 35e |
| 35f | CLI request-construction error handling | Wave 4 | Resolved | A repository-wide audit found no ignored `http.NewRequest` result: all current CLI request constructors use `http.NewRequestWithContext`, bind the error, and return before `Do` on failure. The complete `internal/cli` test suite passes. | Remove from active backlog |
| 36 | Parent-to-child call-run cancellation propagation | Wave 4 | Resolved | Call steps persist `child_run_id` before waiting; store traversal returns descendants, and `cancelDescendantRuns` performs cycle-guarded breadth-first cancellation. Controller tests cover explicit parent cancellation through child and grandchild, failed-parent finish, and orphan-reaper cascade. | Remove from active backlog |
| 37 | Kubernetes secret interpolation | Wave 5 | Audit required | Current-tree inspection required | Decision required |
| 38 | Kubernetes secret log masking | Wave 5 | Audit required | Current-tree inspection required | Decision required |
| 39 | Kubernetes call step execution | Wave 5 | Audit required | Current-tree inspection required | Decision required |
| 40 | Kubernetes step environment propagation | Wave 5 | Audit required | Current-tree inspection required | Decision required |
| 41 | Kubernetes timeout enforcement | Wave 5 | Audit required | Current-tree inspection required | Decision required |
| 42 | Kubernetes post-hook execution | Wave 5 | Audit required | Current-tree inspection required | Decision required |
| 43 | Kubernetes stderr streaming | Wave 5 | Audit required | Current-tree inspection required | Decision required |
| 44 | Shared host/Kubernetes orchestration and parity coverage | Wave 5 | Audit required | Current-tree inspection required | Decision required |
| 24 | Audit logging | Wave 5 | Audit required | Current-tree inspection required | Decision required |
| L1 | Missing favicon | Wave 5 | Audit required | Current-tree inspection required | Decision required |
| L2 | Mixed UI language and HTML language metadata | Wave 5 | Audit required | Current-tree inspection required | Decision required |
| L3 | login command server configuration parity | Wave 5 | Audit required | Current-tree inspection required | Decision required |
| L4 | AppSource mixed-kind documentation and sync-status visibility | Wave 5 | Audit required | Current-tree inspection required | Decision required |
| CTX-K8S | Kubernetes validation-environment notes | Wave 5 | Audit required | Current-tree inspection required | Decision required |
| CTX-HEALTHY | Verified-healthy feature list | Wave 5 | Audit required | Current-tree inspection required | Decision required |
| CTX-ENV | General validation-environment notes | Wave 5 | Audit required | Current-tree inspection required | Decision required |
