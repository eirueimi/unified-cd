# Active Backlog Audit Matrix

Audit baseline: `1e46459` (2026-07-26)

This is a temporary review artifact. Every row must be decided before the
active backlog is rewritten, and this file must be removed from the final
tree.

## Decision Rules

- Resolved: current implementation and tests cover the legacy problem.
- Accepted behavior: current behavior is intentional and documented.
- Environment-only: the observation belongs to a historical validation setup.
- Open: the problematic behavior is still present.
- Partial: a concrete part remains after verified fixes.
- Needs verification: static evidence is insufficient; the retained task must
  state an exact experiment and success condition.

## Candidate Ledger

| Key | Legacy scope | Wave | Outcome | Evidence | Final action |
|---|---|---:|---|---|---|
| 1 | Step failure or skip can wedge a run | Wave 1 | Resolved | `internal/store/migrations/001_init.up.sql` constraint `step_reports_status_check` accepts `Skipped` and `WaitingApproval`; `internal/agent/retry.go` `retryUntilSuccess` stops on permanent 4xx errors; `test/e2e/failing_step_test.go` `TestFailingStepSkipsRestAndFailsRun` proves a failed first step leaves the next `Skipped` and the run terminal `Failed`. | Remove from active backlog |
| 2 | Required/default parameter enforcement | Wave 1 | Resolved | `internal/controller/params.go` `resolveParams` rejects missing required inputs and injects defaults; `internal/controller/api_runs.go` run creation, `internal/controller/api_webhooks.go` webhook ingress, and `internal/controller/scheduler.go` schedule firing all call it. `internal/controller/params_test.go`, `api_runs_test.go`, `api_webhooks_test.go`, and `scheduler_schedule_test.go` cover missing required inputs and defaults; `docs/jobs.md` input fields match the runtime contract. | Remove from active backlog |
| 3 | Conditional syntax mismatch and fail-open behavior | Wave 1 | Partial | `docs/jobs.md` table under `Available variables` still lists Go-template expressions `{{ .Params.NAME }}` and `{{ .Steps.NAME.Outputs.KEY }}` as available in `if`, contradicting its CEL section. `internal/agent/orchestrator.go` `RunClaim` passes `step.If` directly to `internal/dsl/condition.go` `EvalCondition`, which returns true with an error on CEL compile/evaluation failure; `internal/dsl/condition_test.go` `TestEvalCondition_InvalidExpr` and `TestEvalCondition_NonBoolResult` require that fail-open result. | Retain as 3 |
| 4 | Standard-agent artifact upload relative paths | Wave 1 | Resolved | `internal/agent/orchestrator.go` `executeUploadArtifact` resolves paths through `ExecBackend.ResolveArtifactPath`; `internal/agent/backend_host.go` `hostBackend.ResolveArtifactPath` resolves against the run workspace via `resolveWorkspacePath`. `internal/agent/agent_test.go` `TestAgent_UploadArtifact_RelativePath` uploads `out.txt` from the workspace while the process cwd lacks it; `docs/jobs.md` documents relative artifact paths. | Remove from active backlog |
| 5 | Approval visibility in run show | Wave 1 | Resolved | `internal/agent/approval.go` `WaitForApproval` reports `WaitingApproval`, allowed by `step_reports_status_check`; `internal/controller/planned_steps.go` `plannedSteps` and `mergedRunSteps` expose unreported approval steps as `Pending`, and `internal/cli/run.go` `newRunShowCmd` prints the steps endpoint response. `internal/agent/approval_test.go` covers approval reporting/flow, while `internal/controller/planned_steps_test.go` covers planned-step merging and the steps API. | Remove from active backlog |
| 6 | parallel-steps example apply validity | Wave 2 | Resolved | `examples/jobs/parallel-steps.yaml` now represents each concurrent stage as a `parallel:` entry with named child steps; `internal/dsl/examples_parse_test.go` `TestExamplesParse` walks every job example and parses each `kind: Job` through `dsl.Parse`, including this file. | Remove from active backlog |
| 7 | CLI flag spelling in examples and docs | Wave 2 | Resolved | `internal/cli/run.go` `newRunTriggerCmd` registers the repeatable `--param` flag; `examples/jobs/params.yaml`, `docs/cli.md` `run trigger`, `docs/jobs.md` parameter examples, and `docs/getting-started.md` all use repeated `--param key=value`, with no current `--params` usage outside historical planning documents. | Remove from active backlog |
| 8 | Artifact step display name | Wave 2 | Needs verification | `internal/agent/orchestrator.go` `executeUploadArtifact` and `executeDownloadArtifact` include `StepName: step.DisplayName()` and `StageIndex: step.StageIndex` in Running and terminal reports; `internal/agent/agent_test.go` `TestAgent_UploadArtifact_RelativePath` asserts every upload report is named `uploadOut` instead of falling back to `step[0]`. The download half has only static implementation evidence. Add an exact `executeDownloadArtifact` test that captures its Running and terminal reports; success requires both reports to have `StepName` equal to the configured download display name and `StageIndex` equal to that step's configured stage index. | Retain as 8 |
| 9 | Workspace reuse between runs | Wave 2 | Accepted behavior | `internal/agent/workspace.go` `claimWorkDir` partitions slot workspaces by sanitized job name, while `prepareWorkspace` intentionally preserves same-job files unless cleaning is requested or execution mode changes; `internal/agent/workspace_test.go` covers job-scoped paths, carry-over, cleaning, and mode-flip reset. `docs/agents.md` `Workspace lifecycle` explicitly documents same-job carry-over, `--clean-workspace`, `podTemplate.cleanWorkspace`, and the secret-file risk. | Remove from active backlog |
| 9c | Windows cancellation and orphaned child processes | Wave 2 | Partial | `internal/agent/exec_windows.go` `assignJob` uses a Job Object with `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE`, and `internal/agent/runner_test.go` `TestRunStep_CancelKillsGrandchild` verifies the exercised process tree stops; `internal/agent/orchestrator.go` reports an interrupted step as `Cancelled`, covered by `internal/agent/agent_cancel_test.go` `TestExecuteRun_CancelledStepReportsCancelledStatus` and documented in `docs/jobs.md`. However, `internal/agent/exec_tree.go` `runTreeKilled` assigns the Job Object only after `cmd.Start` and explicitly retains both a child-spawn escape window and a single-process fallback when assignment fails, so orphan prevention is not complete. | Retain as 9c |
| 10 | Cancelled-run log visibility in the Web UI | Wave 2 | Needs verification | `web/src/routes/RunDetail.svelte` has a single reactive `runID` initialization path and always starts one SSE connection, including for terminal `Cancelled` runs; `internal/controller/sse.go` `handleRunEvents` replays existing logs before emitting any terminal status. `web/src/routes/RunDetail.test.js` pins one `/events` connection per run but exercises a Running run with an empty stream, while `internal/controller/api_runs_test.go` `TestAPI_RunEvents_SSE_ReceivesExistingLogs` backfills a Succeeded run without Web rendering. Add an exact component or browser experiment that loads a pre-cancelled run whose SSE backfill contains historical log lines and then closes with terminal status; success requires those historical lines to remain rendered in the run detail after terminal closure. | Retain as 10 |
| 11 | CLI run cancel command | Wave 2 | Resolved | `internal/cli/run.go` `newRunCmdWithClient` registers `newRunCancelCmd`, which POSTs `/api/v1/runs/{id}/cancel`; `internal/cli/run_test.go` `TestRunCancel_Success` verifies method, path, authorization, and confirmation output. `docs/cli.md` `run cancel`, `docs/troubleshooting.md`, and `docs/operations.md` document the command and cancellation behavior. | Remove from active backlog |
| 12 | Agent inventory recovery after controller database loss | Wave 2 | Partial | `internal/controller/api_agent.go` `handleAgentClaim` calls `store.UpsertAgentOnClaim` before polling, and `internal/controller/api_agent_test.go` `TestAgentAPI_Claim_UpsertsUnregisteredAgent` proves an authenticated agent missing only its inventory row reappears in `ListAgents`; `internal/store/postgres.go` `UpsertAgentOnClaim` preserves richer registration data. A fresh database also loses the credential hashes listed in `docs/operations.md`; `internal/controller/agent_auth.go` authenticates before the claim handler via `GetAgentCredentialForAuth`, so claim-time recovery cannot run until the agent is re-enrolled. `docs/operations.md` both promises automatic reappearance on a fresh database and separately requires re-enrollment, leaving the full-loss recovery contract unresolved. | Retain as 12 |
| A | Orphaned-run reaper | Wave 2 | Resolved | `cmd/controller/main.go` starts `controller.RunStuckRunReaper`; `internal/controller/stuckrun_reaper.go` leader-elects, finds stale or missing-agent Running runs through `store.ListStuckRunIDs`, marks each `Failed`, releases locks through `MarkRunFinished`, and cascade-cancels descendants. `internal/controller/stuckrun_reaper_test.go` covers leader/follower behavior, `internal/store/postgres_stuckrun_test.go` covers selection, and `docs/high-availability.md` `Orphaned-Run Recovery` documents timings and fail-not-requeue semantics. | Remove from active backlog |
| 13 | Default Kubernetes sidecar image pullability | Wave 3 | Audit required | Current-tree inspection required | Decision required |
| 14 | Distroless sidecar versus shell transfer | Wave 3 | Audit required | Current-tree inspection required | Decision required |
| 15 | Kubernetes working directory | Wave 3 | Audit required | Current-tree inspection required | Decision required |
| 16 | Kubernetes single-file artifact upload and failure reporting | Wave 3 | Audit required | Current-tree inspection required | Decision required |
| 21 | Sidecar idle mode without S3 | Wave 3 | Audit required | Current-tree inspection required | Decision required |
| 17 | Schedule apply support | Wave 3 | Audit required | Current-tree inspection required | Decision required |
| 18 | Artifact download default destination | Wave 3 | Audit required | Current-tree inspection required | Decision required |
| 19 | call step self-deadlock | Wave 3 | Audit required | Current-tree inspection required | Decision required |
| 20b | CLI configuration precedence | Wave 3 | Audit required | Current-tree inspection required | Decision required |
| 20 | SSO direct hash-route rendering | Wave 3 | Audit required | Current-tree inspection required | Decision required |
| 22 | Windows Job Object handle lifecycle | Wave 4 | Audit required | Current-tree inspection required | Decision required |
| 23 | Agent label deletion on re-registration | Wave 4 | Audit required | Current-tree inspection required | Decision required |
| 25 | AppSource first-sync prune after upgrade | Wave 4 | Audit required | Current-tree inspection required | Decision required |
| 26 | Artifact-name path traversal | Wave 4 | Audit required | Current-tree inspection required | Decision required |
| 27 | Heartbeat lifecycle while draining | Wave 4 | Audit required | Current-tree inspection required | Decision required |
| 28 | Pod GC handling of controller errors | Wave 4 | Audit required | Current-tree inspection required | Decision required |
| 29 | Database migration coverage for pre-squash installations | Wave 4 | Audit required | Current-tree inspection required | Decision required |
| 30 | Template shell argument quoting | Wave 4 | Audit required | Current-tree inspection required | Decision required |
| 31 | Matrix cardinality limit before Cartesian allocation | Wave 4 | Audit required | Current-tree inspection required | Decision required |
| 32 | Cache-path expansion parity | Wave 4 | Audit required | Current-tree inspection required | Decision required |
| 32a | Empty parameters bypassing defaults | Wave 4 | Audit required | Current-tree inspection required | Decision required |
| 32b | Standard-agent cache paths relative to process cwd | Wave 4 | Audit required | Current-tree inspection required | Decision required |
| 32c | Kubernetes cache path traversal | Wave 4 | Audit required | Current-tree inspection required | Decision required |
| 33a | SSO round-trip deep-link preservation | Wave 4 | Audit required | Current-tree inspection required | Decision required |
| 33b | AppSource sync status recovery | Wave 4 | Audit required | Current-tree inspection required | Decision required |
| 33c | Credential-bearing AppSource repository URL disclosure | Wave 4 | Audit required | Current-tree inspection required | Decision required |
| 33d | Late finish and step reports after terminal state | Wave 4 | Audit required | Current-tree inspection required | Decision required |
| 33e | Published quickstart image health-check compatibility | Wave 4 | Audit required | Current-tree inspection required | Decision required |
| 34 | Stale failing automated tests | Wave 4 | Audit required | Current-tree inspection required | Decision required |
| 35a | Heartbeat cancellation assertion | Wave 4 | Audit required | Current-tree inspection required | Decision required |
| 35b | AppSource migration-path coverage | Wave 4 | Audit required | Current-tree inspection required | Decision required |
| 35c | Matrix variant output aggregation coverage | Wave 4 | Audit required | Current-tree inspection required | Decision required |
| 35d | Human artifact-download authorization coverage | Wave 4 | Audit required | Current-tree inspection required | Decision required |
| 35e | CLI HTTP method assertions | Wave 4 | Audit required | Current-tree inspection required | Decision required |
| 35f | CLI request-construction error handling | Wave 4 | Audit required | Current-tree inspection required | Decision required |
| 36 | Parent-to-child call-run cancellation propagation | Wave 4 | Audit required | Current-tree inspection required | Decision required |
| 37 | Kubernetes secret interpolation | Wave 5 | Audit required | Current-tree inspection required | Decision required |
| 38 | Kubernetes secret log masking | Wave 5 | Audit required | Current-tree inspection required | Decision required |
| 39 | Kubernetes call step execution | Wave 5 | Audit required | Current-tree inspection required | Decision required |
| 40 | Kubernetes step environment propagation | Wave 5 | Audit required | Current-tree inspection required | Decision required |
| 41 | Kubernetes timeout enforcement | Wave 5 | Audit required | Current-tree inspection required | Decision required |
| 42 | Kubernetes post-hook execution | Wave 5 | Audit required | Current-tree inspection required | Decision required |
| 43 | Kubernetes stderr streaming | Wave 5 | Audit required | Current-tree inspection required | Decision required |
| 44 | Shared host/Kubernetes orchestration and parity coverage | Wave 5 | Audit required | Current-tree inspection required | Decision required |
| 24 | Audit logging | Wave 5 | Audit required | Current-tree inspection required | Decision required |
| L1 | Missing favicon | Wave 5 | Audit required | Current-tree inspection required | Decision required |
| L2 | Mixed UI language and HTML language metadata | Wave 5 | Audit required | Current-tree inspection required | Decision required |
| L3 | login command server configuration parity | Wave 5 | Audit required | Current-tree inspection required | Decision required |
| L4 | AppSource mixed-kind documentation and sync-status visibility | Wave 5 | Audit required | Current-tree inspection required | Decision required |
| CTX-K8S | Kubernetes validation-environment notes | Wave 5 | Audit required | Current-tree inspection required | Decision required |
| CTX-HEALTHY | Verified-healthy feature list | Wave 5 | Audit required | Current-tree inspection required | Decision required |
| CTX-ENV | General validation-environment notes | Wave 5 | Audit required | Current-tree inspection required | Decision required |
