apiVersion: unified-cd/v1
kind: Job
metadata:
  name: edge-w4-tick
spec:
  agentSelector:
    - kind:kubernetes
  podTemplate:
    spec:
      containers:
        - name: job
          image: alpine:3.20
  steps:
    - name: probe
      run: |
        echo "w4-tick-ran"
        echo "W4-POD-HOSTNAME=$(hostname)"
